{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "Root config - Backend service uses backend/railway.toml for config-as-code",
  "$comment2": "When Railway finds backend/railway.toml, it automatically uses backend/ as root directory"
}
