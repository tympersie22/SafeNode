# Test environment — LOCAL database only. Never point this at production.
# Loaded by tests/env.ts (jest setupFiles) before app config, so it pre-empts
# the Neon URL in .env / .env.local during test runs.
NODE_ENV=test
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/safenode_test?connect_timeout=5
JWT_SECRET=test-only-jwt-secret-not-used-in-production-000000
ENCRYPTION_KEY=dGVzdC1lbmNyeXB0aW9uLWtleS0zMi1ieXRlcy1wYWQwMDA=
