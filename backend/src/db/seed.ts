/**
 * Database Seed
 *
 * Demo / seed accounts have been removed from the codebase. This is
 * intentionally a no-op so the boot-time and CLI seeding paths stay wired but
 * never create any accounts. Create accounts through the normal signup flow.
 */

export async function seedDatabase(): Promise<void> {
  console.log('ℹ️  No seed data configured — skipping (demo accounts removed).')
}
